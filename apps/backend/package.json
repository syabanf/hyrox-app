{
  "name": "@hyrox/backend",
  "version": "0.1.0",
  "private": true,
  "description": "Go + PostgreSQL API for the NuHabit studio operating system",
  "scripts": {
    "dev": "make run",
    "build": "make build",
    "test": "go test ./...",
    "typecheck": "go vet ./...",
    "lint": "make lint",
    "migrate": "go run ./cmd/migrate",
    "seed": "go run ./cmd/seed"
  },
  "engines": {
    "go": ">=1.25"
  }
}
